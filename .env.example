# MCP Server Configuration
# Copy this file to .env and configure
#
# Deployment Mode: STANDALONE
# STANDALONE MODE - Auth handled by this MCP via Key Vault secrets

# ============================================================================
# SERVER SETTINGS (for local development)
# ============================================================================

# Mode: "local" (stdio) or "remote" (HTTP/SSE)
MCP_MODE=local

# Remote mode settings
PORT=3000
ALLOWED_ORIGINS=*
LOG_LEVEL=info

# ============================================================================
# AZURE KEY VAULT (Required - everything else is fetched from here)
# ============================================================================

# Key Vault name (REQUIRED)
AZURE_KEYVAULT_NAME=

# Key Vault URL (auto-constructed if not provided)
# AZURE_KEYVAULT_URL=https://<keyvault-name>.vault.azure.net/

# ============================================================================
# KEY VAULT SECRETS (One-time org setup - not per MCP)
# ============================================================================
# These secrets should already exist in your organization's Key Vault:
#
# Infrastructure (shared by all MCPs):
#   azure-acr-name              : Container Registry name
#   azure-resource-group        : Resource group for Container Apps
#   azure-container-environment : Container Apps environment name
#   mcp-gateway-url             : MCP Gateway URL (for gateway mode)
#
# Authentication (for standalone mode):
#   auth-mode                   : "azure_ad", "api_key", or "none"
#   azure-tenant-id             : Azure AD tenant ID
#   azure-client-id             : App registration client ID
#   azure-allowed-groups        : Comma-separated group IDs (optional)
#   api-keys                    : Comma-separated API keys (optional)
#
# Your MCP's credentials (add as needed):
#   (e.g., jira-api-token, database-url, etc.)

# ============================================================================
# LOCAL DEV ONLY
# ============================================================================
# For local development without Key Vault:
# SECRETS_MODE=env
# AUTH_MODE=none

# ============================================================================
# OPENFDA API KEY (Optional)
# ============================================================================
# Get your free key at: https://open.fda.gov/apis/authentication/
# Without key: 240 requests/min, 1,000 requests/day per IP
# With key: 240 requests/min, 120,000 requests/day per key
#
# Can also be stored in Key Vault as "openfda-api-key"
OPENFDA_API_KEY=
