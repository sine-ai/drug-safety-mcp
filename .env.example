# Drug Safety MCP Server Configuration
# Copy this file to .env for local development

# ============================================================================
# SERVER MODE
# ============================================================================

# Mode: "local" (stdio) or "remote" (Streamable HTTP)
MCP_MODE=local

# Remote mode settings (only used when MCP_MODE=remote)
PORT=3000

# ============================================================================
# OPENFDA API KEY (Optional)
# ============================================================================
# Get your free key at: https://open.fda.gov/apis/authentication/
#
# Without key (free mode): 240 requests/min, 1,000 requests/day per IP
# With key: 240 requests/min, 120,000 requests/day per key
#
# For production, set this in your MCP client config instead of here.
OPENFDA_API_KEY=

# ============================================================================
# LOGGING
# ============================================================================

# Log level: debug, info, warn, error
LOG_LEVEL=info
