# Drug Safety MCP Server Configuration
# Copy this file to .env for local development

# ============================================================================
# SERVER MODE
# ============================================================================

# Mode: "local" (stdio) or "remote" (Streamable HTTP with OAuth)
MCP_MODE=local

# Remote mode settings
PORT=3000
BASE_URL=https://your-server.com

# ============================================================================
# OAUTH 2.0 VIA MCP GATEWAY
# ============================================================================
# Authentication is handled by MCP Gateway which supports:
# - Microsoft work/school accounts (Azure AD)
# - Microsoft personal accounts (Outlook, Hotmail, Xbox)
# - Google accounts
#
# Users sign in via Gateway, and this server validates their tokens.

# Enable OAuth (required for remote mode in production)
OAUTH_ENABLED=false

# MCP Gateway URL (handles OAuth flow)
# Set this to your deployed MCP Gateway instance
MCP_GATEWAY_URL=

# ============================================================================
# OPENFDA API KEY (Optional)
# ============================================================================
# Get your free key at: https://open.fda.gov/apis/authentication/
# Without key: 240 requests/min, 1,000/day per IP
# With key: 240 requests/min, 120,000/day
OPENFDA_API_KEY=

# ============================================================================
# OTHER SETTINGS
# ============================================================================

# CORS origin (set specific origin in production)
CORS_ORIGIN=*

# Log level: debug, info, warn, error
LOG_LEVEL=info
