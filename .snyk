# Snyk policy file - document accepted security risks
# https://docs.snyk.io/snyk-cli/commands/ignore

version: v1.25.0

ignore:
  # HTTP is used internally - HTTPS termination happens at load balancer/ingress
  # This is standard practice for containerized apps (Azure Container Apps, K8s, etc.)
  # External traffic: User --HTTPS--> Azure Ingress --HTTP--> Container
  CWE-319:
    - '*':
        reason: 'HTTP used internally only. HTTPS termination at Azure Container Apps ingress layer.'
        expires: '2027-01-01T00:00:00.000Z'
