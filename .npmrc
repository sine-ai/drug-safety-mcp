# NPM Registry Configuration
# For private registry, configure via environment or Azure Artifacts credential provider
# Example: registry=https://pkgs.dev.azure.com/YOUR_ORG/_packaging/YOUR_FEED/npm/registry/
# always-auth=true
